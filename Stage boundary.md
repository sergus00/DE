**Stage boundary** в Spark — это “граница” внутри DAG, которая отделяет одну стадию вычислений от другой.  
Эта граница появляется каждый раз, когда Spark встречает операцию, требующую **shuffle** (wide transformation).